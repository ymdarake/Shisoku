# ソースコード インデックス

このメモリは、Shisokuプロジェクトのソースコード構造へのナビゲーションを提供します。

## 📁 ディレクトリ構造

```
src/
├── component/          # UIコンポーネント（11ファイル）
├── service/            # ビジネスロジック（2ファイル）
├── usecase/            # ユースケース層（2ファイル）
├── domain/             # ドメインモデル
│   ├── preferences/    # 設定関連
│   └── ranking/        # ランキング関連
├── repository/         # データ永続化
│   ├── memory/         # インメモリ実装
│   └── localStorage/   # localStorage実装
├── context/            # React Context（2ファイル）
├── hook(s)/            # カスタムフック（4ファイル）
├── constant/           # 定数定義（4ファイル）
├── utils/              # ユーティリティ（2ファイル）
├── styles/             # CSS（1ファイル）
├── __tests__/          # テスト（23ファイル）
├── App.tsx             # ルートコンポーネント
├── index.tsx           # エントリーポイント
└── types.ts            # TypeScript型定義
```

---

## 🎯 主要ファイル

### エントリーポイント

#### `src/index.tsx`
- **役割**: アプリケーションのエントリーポイント
- **内容**: Reactのルートレンダリング、Context Providerの設定

#### `src/App.tsx`
- **役割**: ルートコンポーネント、状態管理の中心
- **状態**: gameState、difficulty、currentProblem、results
- **フロー**: idle → countdown → playing → finished → ranking

---

## 🧩 Component層（src/component/）

### 画面コンポーネント

#### `StartScreen.tsx`
- **役割**: スタート画面
- **機能**: 難易度選択、ルール表示、ゲーム開始

#### `CountdownScreen.tsx`
- **役割**: カウントダウン画面
- **機能**: 3,2,1カウントダウン、問題事前生成（10問）

#### `GameScreen.tsx`
- **役割**: ゲームプレイ画面
- **機能**: 数式入力、自動判定、問題遷移、タイマー管理

#### `EndScreen.tsx`
- **役割**: 結果画面
- **機能**: スコア表示、ランキング保存、リトライ

#### `RankingScreen.tsx`
- **役割**: ランキング画面
- **機能**: ランキング一覧表示、ソート（スコア降順→時間昇順）

### UI部品コンポーネント

#### `Header.tsx`
- **役割**: ヘッダー
- **機能**: BGM/SFX切り替え、ダークモード、言語切り替え

#### `ProblemDisplay.tsx`
- **役割**: 問題表示
- **最適化**: React.memo使用

#### `InputDisplay.tsx`
- **役割**: 入力式表示
- **最適化**: React.memo使用

#### `MessageArea.tsx`
- **役割**: メッセージ表示（正解/不正解/無効）
- **最適化**: React.memo使用

#### `Controls.tsx`
- **役割**: 数字・演算子ボタン、Clear/Backspace/Skip
- **機能**: クリック入力、使用済み数字の視覚フィードバック

#### `DifficultySelector.tsx`
- **役割**: 難易度選択UI
- **機能**: Easy/Normal/Hard選択、ハイライト表示

#### `Rules.tsx`
- **役割**: ルール表示コンポーネント（再利用可能）
- **使用箇所**: StartScreen

#### `icons.tsx`
- **役割**: SVGアイコン定義
- **アイコン**: BGM、SFX、ダークモード、言語切り替えアイコン

---

## ⚙️ Service層（src/service/）

### `gameLogic.ts`
- **役割**: 問題生成・式評価ロジック
- **主要関数**:
  - `generateProblem(difficulty)`: 1問生成（全順列・演算子・括弧パターンを探索）
  - `generateProblems(count, difficulty)`: 複数問生成
  - `safeEvaluateExpression(expression)`: 式の安全な評価
- **制約**: 
  - 結果が0-9（または難易度による）の整数
  - 割り算は整数結果のみ有効
  - 最大5000回試行

### `audio.ts`
- **役割**: Tone.js音楽システム
- **BGM構成**: FM合成シンセ + ベース + ドラム（キック・ハイハット）
- **SFX**: クリック、正解、不正解、無効、カウントダウン、スタート音
- **機能**: ボリューム管理（BGM/SFX個別ON/OFF）

---

## 🎯 UseCase層（src/usecase/）

### `saveScore.ts`
- **役割**: スコア保存ユースケース
- **処理**: ランキングデータの保存、最大10件制限、ソート

### `loadRankings.ts`
- **役割**: ランキング読み込みユースケース
- **処理**: ランキングデータの取得

---

## 🏛️ Domain層（src/domain/）

### Preferences（設定）

#### `domain/preferences/type.ts`
- **型定義**: Preferences（language、darkMode、bgmEnabled、sfxEnabled）

#### `domain/preferences/PreferencesRepository.ts`
- **インターフェース**: PreferencesRepository
- **メソッド**: get、update

### Ranking（ランキング）

#### `domain/ranking/type.ts`
- **型定義**: RankingEntry（date、score、time、difficulty）

#### `domain/ranking/RankingRepository.ts`
- **インターフェース**: RankingRepository
- **メソッド**: getAll、save

---

## 💾 Repository層（src/repository/）

### Memory実装（テスト用）

#### `repository/memory/InMemoryPreferencesRepository.ts`
- **役割**: インメモリ設定リポジトリ
- **用途**: テスト用

#### `repository/memory/InMemoryRankingRepository.ts`
- **役割**: インメモリランキングリポジトリ
- **用途**: テスト用

### LocalStorage実装（本番）

#### `repository/localStorage/LocalStoragePreferencesRepository.ts`
- **役割**: localStorage設定リポジトリ
- **キー**: 'mathPuzzlePreferences'

#### `repository/localStorage/LocalStorageRankingRepository.ts`
- **役割**: localStorageランキングリポジトリ
- **キー**: 'mathPuzzleRanking'

---

## 🔌 Context（src/context/）

### `PreferencesRepositoryContext.tsx`
- **役割**: PreferencesRepositoryの提供
- **使用**: useMemo でインスタンスキャッシュ

### `RankingRepositoryContext.tsx`
- **役割**: RankingRepositoryの提供
- **使用**: useMemo でインスタンスキャッシュ

---

## 🪝 Hooks（src/hook(s)/）

### `hook/useRanking.ts`
- **役割**: ランキング管理のカスタムフック
- **機能**: ランキング取得、スコア保存、リポジトリ利用

### `hooks/useDarkMode.ts`
- **役割**: ダークモード管理
- **機能**: テーマ切り替え、localStorage連携

### `hooks/useKeyboardInput.ts`
- **役割**: キーボード入力処理
- **機能**: 数字・演算子・括弧・Enter・Escape・Backspace

---

## 📊 Constants（src/constant/）

### `locales.ts`
- **役割**: 多言語テキスト定義
- **言語**: 日本語（ja）、英語（en）
- **内容**: UI全体のテキスト、ルール、メッセージ

### `difficulty.ts`
- **役割**: 難易度設定の定数
- **定義**: Easy/Normal/Hardの数字範囲・目標範囲

### `game.ts`
- **役割**: ゲーム定数
- **定義**: TOTAL_QUESTIONS（10問）

### `keyboardMap.ts`
- **役割**: キーボードマッピング
- **定義**: キーコードとゲーム入力の対応

---

## 🛠️ Utils（src/utils/）

### `formatTime.ts`
- **役割**: 時間フォーマット（ミリ秒 → MM:SS.mmm）
- **使用箇所**: EndScreen、RankingScreen

### `share.ts`
- **役割**: シェア機能（SNS共有、クリップボードコピー）
- **使用箇所**: EndScreen

---

## 🎨 Styles（src/styles/）

### `index.css`
- **役割**: グローバルスタイル
- **内容**: Tailwindディレクティブ、カスタムアニメーション（shake-animation）

---

## 🧪 Tests（src/__tests__/）

### テスト構成（23ファイル）

#### コンポーネントテスト（8ファイル）
- `components/ProblemDisplay.test.tsx`
- `components/InputDisplay.test.tsx`
- `components/Controls.test.tsx`
- `components/DifficultySelector.test.tsx`
- `components/Header.test.tsx`
- `components/GameScreen.test.tsx`
- `components/EndScreen.test.tsx`
- `components/RankingScreen.test.tsx`

#### サービステスト（3ファイル）
- `services/gameLogic.test.ts`
- `services/ranking.test.ts`
- `services/preferences.test.ts`

#### ユースケーステスト（2ファイル）
- `usecase/saveScore.test.ts`
- `usecase/loadRankings.test.ts`

#### リポジトリテスト（2ファイル、コントラクトテスト）
- `repository/preferences.contract.test.ts`
- `repository/ranking.contract.test.ts`

#### フックテスト（1ファイル）
- `hook/useRanking.test.tsx`

#### ユーティリティテスト（2ファイル）
- `utils/formatTime.test.ts`
- `utils/share.test.ts`

---

## 🎯 重要な型定義（src/types.ts）

### 主要な型
- `GameState`: 'idle' | 'countdown' | 'playing' | 'finished' | 'ranking'
- `Difficulty`: 'easy' | 'normal' | 'hard'
- `Language`: 'ja' | 'en'
- `Problem`: { numbers: number[], target: number }
- `Token`: { value: string, type: 'number' | 'operator', originalIndex?: number }
- `GameResult`: { correct: boolean, problem: Problem, userInput: string, timeSpent: number }

---

## 🔍 コード探索のヒント

### 新しい画面を追加したい
1. `src/component/` に新規コンポーネント作成
2. `App.tsx` の `GameState` に状態追加
3. 画面遷移ロジックを `App.tsx` に実装

### 新しいビジネスロジックを追加したい
1. `src/service/` に新規サービス作成
2. 必要に応じて `src/usecase/` にユースケース作成
3. `src/__tests__/services/` にテスト追加

### 新しいデータを永続化したい
1. `src/domain/` にドメインモデル定義
2. `src/repository/` にリポジトリ実装（memory + localStorage）
3. `src/context/` にContextを作成
4. `src/__tests__/repository/` にコントラクトテスト追加

### 多言語対応のテキストを追加したい
1. `src/constant/locales.ts` にテキスト追加（ja/en）
2. コンポーネントで `TEXT[language]` で参照

---

## 📊 統計

- **総ファイル数**: 約60ファイル
- **UIコンポーネント**: 11個
- **テストファイル**: 23個
- **テストカバレッジ**: 高（主要ロジックをカバー）

---

*最終更新: 2025-01-16*
